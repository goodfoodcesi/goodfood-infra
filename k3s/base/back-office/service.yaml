apiVersion: v1
kind: Service
metadata:
  name: back-office
  namespace: default
spec:
  selector:
    app: back-office
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP