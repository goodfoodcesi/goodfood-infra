# 05-tyk/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: tyk-gateway
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: tyk-gateway
  ports:
  - port: 8080
    targetPort: 8080